<Window x:Class="Project_Prn212.ManageMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Prn212"
        mc:Ignorable="d"
        Title="ManageMenu" Height="450" Width="800">
    <Grid>
        <!--<DataGrid Name="dgMenuItems" AutoGenerateColumns="False" CanUserAddRows="False" Margin="10" IsReadOnly="True" ItemsSource="{Binding MenuItems}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Tên Món" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Giá" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Loại" Binding="{Binding Category}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Thêm" Margin="10" Click="AddMenuItem_Click"/>
            <Button Content="Sửa" Margin="10" Click="EditMenuItem_Click"/>
            <Button Content="Xóa" Margin="10" Click="DeleteMenuItem_Click"/>
        </StackPanel>-->
    </Grid>
</Window>
